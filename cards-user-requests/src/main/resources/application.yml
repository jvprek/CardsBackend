server:
  port: 10010
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

spring:
  application:
    name: cards-user-requests
  config:
    import: "optional:configserver:${CONFIG_SERVER:http://localhost:8888}"
